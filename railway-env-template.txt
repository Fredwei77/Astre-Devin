# ============================================
# Railway 环境变量配置模板
# ============================================
# 
# 使用方法:
# 1. 复制以下变量到 Railway 控制台
# 2. 替换所有 "your-xxx-here" 为实际值
# 3. 确保所有必需变量都已设置
#
# Railway 控制台路径:
# https://railway.app/dashboard → 选择项目 → Variables 标签
# ============================================

# ============================================
# 🌍 基础配置
# ============================================
NODE_ENV=production
PORT=3000
FRONTEND_URL=https://astredevin.netlify.app

# ============================================
# 🤖 OpenRouter AI API
# ============================================
# 获取地址: https://openrouter.ai → API Keys
# ⚠️ 重要: 使用新密钥，不要使用已泄露的密钥
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-key-here

# ============================================
# 💳 Stripe 支付系统
# ============================================
# 获取地址: https://dashboard.stripe.com → Developers → API keys
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key-here
STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key-here

# Webhook 密钥 (用于支付回调验证)
# 获取地址: Stripe Dashboard → Webhooks → 选择端点 → Signing secret
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret-here

# ============================================
# 🗄️ Supabase 数据库
# ============================================
# 获取地址: https://supabase.com/dashboard → 选择项目 → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key-here

# ============================================
# 🔐 JWT 认证
# ============================================
# 生成命令: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-64-character-random-jwt-secret-here

# ============================================
# 📝 配置说明
# ============================================
#
# 1. NODE_ENV: 设置为 production 启用生产模式
# 2. PORT: Railway 会自动分配，通常保持 3000
# 3. FRONTEND_URL: 你的 Netlify 前端域名
# 4. OPENROUTER_API_KEY: 用于 AI 功能的 API 密钥
# 5. STRIPE_*: 支付处理所需的 Stripe 配置
# 6. SUPABASE_*: 数据库连接配置
# 7. JWT_SECRET: 用户认证的签名密钥
#
# ============================================
# 🚨 安全注意事项
# ============================================
#
# ✅ 使用生产环境密钥 (不是测试密钥)
# ✅ 定期轮换 API 密钥
# ✅ 不要在代码中硬编码密钥
# ✅ 确保 JWT_SECRET 足够复杂
# ✅ 限制 API 密钥权限范围
#
# ============================================
# 🔍 验证配置
# ============================================
#
# 部署后访问以下端点验证配置:
# - GET /health - 服务健康检查
# - GET /api/status - API 状态检查
#
# 如果返回错误，检查对应的环境变量配置
#
# ============================================

# 复制上述配置到 Railway 时的步骤:
# 
# 1. 打开 Railway 项目控制台
# 2. 点击 "Variables" 标签
# 3. 逐个添加变量:
#    - 点击 "New Variable"
#    - 输入变量名 (例如: NODE_ENV)
#    - 输入变量值 (例如: production)
#    - 点击 "Add"
# 4. 重复直到所有变量添加完成
# 5. 保存并重新部署服务